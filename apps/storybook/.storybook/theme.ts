import { create } from 'storybook/theming';

export default create({
  base: 'light',
  brandTitle: '@priver/ui',
  fontBase: '"Inter Variable", sans-serif',
  fontCode: '"JetBrains Mono Variable", monospace',
});
